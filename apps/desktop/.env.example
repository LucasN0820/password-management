# AI Import Service Configuration
# The AI import feature requires a running AI import service

# URL of the AI import service (e.g., http://localhost:3001)
AI_IMPORT_SERVICE_URL=http://localhost:3001

# Shared secret for authenticating with the AI import service
# Must match the AI_IMPORT_SERVICE_SECRET in the service's .env
AI_IMPORT_SERVICE_SECRET=
