Slider — single-thumb range; ink-filled track, paper thumb. Drag or click anywhere on the track. Controlled via `value` + `onValueChange`.

```jsx
<Slider value={len} min={4} max={64} onValueChange={setLen} />
```
