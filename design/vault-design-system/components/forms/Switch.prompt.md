Switch — boolean pill toggle; checked fills with ink, thumb slides. Controlled via `checked` + `onCheckedChange`.

```jsx
<Switch checked={on} onCheckedChange={setOn} />
<Switch size="sm" checked={false} />
```
