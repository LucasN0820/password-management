Badge — small pill for status or labels. `tone="mono"` renders a square monospace tag for crypto labels.

```jsx
<Badge>最受欢迎</Badge>
<Badge tone="success">Strong</Badge>
<Badge tone="mono">AES-256-GCM</Badge>
```
