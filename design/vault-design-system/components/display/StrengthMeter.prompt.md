StrengthMeter — the generator's strength bar; pass a 0–100 `value`, hue shifts weak→medium→strong. `compact` hides the label.

```jsx
<StrengthMeter value={86} />
<StrengthMeter value={42} compact />
```
